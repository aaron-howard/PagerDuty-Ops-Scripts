# DEPRECATED: replaced by ci.yml (real lint/test/security gates).
# This file is kept only because the old demo workflow referenced deleted
# scripts and advertised no real checks. Safe to delete.
name: Demo (deprecated)
on:
  workflow_dispatch:
jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Deprecated — see ci.yml"
